// String
let address = 'Yogyakarta';
console.log(address);

// Number
const score = 0.1 + 0.2;
const isScoreValid = 0.3 == score;
console.log(score);
console.log(isScoreValid);

// Boolean
let isRainy = true;
console.log(isRainy);

// Null
let nullable = null;
console.log(nullable);

// Undefined
let Undefined;
undefined = undefined;
console.log(undefined);

// Notes:
// null, undefined is SVGMaskElement, null is for variable that null  or empty
// undefined is variable that has not defined and it is same to boolean false
